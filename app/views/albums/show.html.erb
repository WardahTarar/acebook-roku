<%=@album.name%>

<%= button_to 'Upload Photos', {:controller => "albums", :action => "new_photo", :id => @album.id}, :method=>:get, class: "main_button button_new" %>

<% @album.photos.each do |photo| %>
  <%= image_tag url_for(photo), class: "photos", height: '300' %>
<% end %>
